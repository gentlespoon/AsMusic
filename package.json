{
  "name": "asmusic",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter asmusic-web dev",
    "build": "scripts/build.sh",
    "lint": "pnpm --filter asmusic-web lint",
    "test": "vitest run",
    "test:watch": "vitest",
    "cap:sync": "pnpm --filter asmusic-web cap:sync",
    "cap:open:ios": "pnpm --filter asmusic-web cap:open:ios",
    "build:release": "bash scripts/build.sh"
  },
  "devDependencies": {
    "happy-dom": "^17.6.3",
    "vitest": "^3.2.7"
  }
}
