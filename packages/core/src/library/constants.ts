/** Placeholder until Subsonic `musicFolderId` selection exists (see `apps/web/NOTE.md`). */
export const DEFAULT_LIBRARY_ID = 'default';

/** Single canonical size for disk cache and all `getCoverArt` network fetches. */
export const CANONICAL_COVER_ART_SIZE = 512;
